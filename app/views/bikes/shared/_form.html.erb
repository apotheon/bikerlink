<%= form_for @bike do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true, autocomplete: :name %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, autocomplete: :description, size: '80x15' %>
  </div>

  <!-- % if @bike.bikepic.url % -->
    <div><!-- %= image_tag @bike.bikepic.url % --></div>
  <!-- % else % -->
    <div class="field">
      <!-- %= f.label :bikepic, 'Bike Pic' % --><br />
      <!-- %= f.file_field :bikepic % -->
    </div>
  <!-- % end % -->

  <div class="actions">
    <%= f.submit 'Submit', class: :submit %>
  </div>
<% end %>
